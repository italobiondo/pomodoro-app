APP_ENV=production
PUBLIC_DOMAIN=pomodoroplus.com.br

# Backend
PORT=4000
FRONTEND_URL=https://pomodoroplus.com.br

# Postgres (prod)
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql://pomodoro:changeme@postgres:5432/pomodoro_db?schema=public

# Redis (prod)
REDIS_URL=redis://redis:6379

# Auth Google (prod)
GOOGLE_CLIENT_ID=changeme
GOOGLE_CLIENT_SECRET=changeme
GOOGLE_CALLBACK_URL=https://pomodoroplus.com.br/api/auth/google/callback

# JWT
JWT_ACCESS_TOKEN_SECRET=changeme
JWT_ACCESS_TOKEN_EXPIRES_IN=604800

# Mercado Pago (prod)
MERCADO_PAGO_ACCESS_TOKEN=changeme
MERCADO_PAGO_WEBHOOK_SECRET=changeme

# Throttling
THROTTLE_TTL=60
THROTTLE_LIMIT=120

# Frontend (Next) - em prod vamos usar /api no mesmo domínio
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_APP_ENV=production
